class UserContent {
  constructor(id, username, email, role, enabled) {
    this.id = id;
    this.username = username;
    this.email = email;
    this.role = role;
    this.enabled = enabled;
  }
};

export default UserContent;
